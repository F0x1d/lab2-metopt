# Поиск глобального минимума функции методом Пиявского-Шуберта

Программа для поиска глобального экстремума липшицевой функции одной переменной на заданном отрезке.

## 📚 Теоретическая основа

### Липшицевость функции

Функция `f(x)` называется **липшицевой** на отрезке `[a, b]`, если существует константа `L > 0` (константа Липшица), такая что для любых `x₁, x₂ ∈ [a, b]`:

```
|f(x₁) - f(x₂)| ≤ L · |x₁ - x₂|
```

Это означает, что функция не может изменяться слишком быстро - её "крутизна" ограничена константой `L`.

### Метод Пиявского-Шуберта

Алгоритм основан на построении **вспомогательных функций** (нижних оценок), которые аппроксимируют исходную функцию снизу.

#### Принцип работы:

1. **Инициализация**: Вычисляем функцию на концах отрезка `[a, b]`

2. **Оценка константы Липшица**: По имеющимся точкам `(xᵢ, f(xᵢ))` оценка:
   ```
   L = r · max{|f(xᵢ) - f(xⱼ)| / |xᵢ - xⱼ|}
   ```
   где `r > 1` - коэффициент надежности (обычно 1.5-3.0)

3. **Построение вспомогательных функций**: Для каждой пары соседних точек `(xᵢ, f(xᵢ))` и `(xᵢ₊₁, f(xᵢ₊₁))` строим функцию:
   ```
   R(x) = max{f(xᵢ) - L·(x - xᵢ), f(xᵢ₊₁) - L·(xᵢ₊₁ - x)}
   ```
   Это две "конусные" функции, пересекающиеся в точке минимума.

4. **Выбор новой точки**: Находим глобальный минимум всех вспомогательных функций:
   ```
   x* = argmin{R(x)}
   ```

5. **Критерий останова**: Останавливаемся, когда:
   ```
   min{f(xᵢ)} - min{R(x)} < ε
   ```
   где `ε` - заданная точность.

### Почему метод работает?

- Вспомогательные функции `R(x)` всегда лежат **ниже** истинной функции `f(x)`
- Минимум `R(x)` дает **нижнюю оценку** глобального минимума
- По мере добавления точек оценка становится всё точнее
- Метод **гарантированно сходится** к глобальному минимуму (для липшицевых функций)

## 🚀 Установка и запуск

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Требуется Python 3.7+

### Запуск тестов

Запуск всех тестов с функциями Растригина, Экли и пользовательской функцией:

```bash
python test_functions.py
```

Это создаст директорию `results/` с графиками для всех тестовых функций.

### Интерактивный режим

Для ввода собственной функции:

```bash
python main.py
```

Программа запросит:
- Функцию `f(x)` (строка, например: `x**2 + np.sin(x)`)
- Границы отрезка `[a, b]`
- Точность `ε`

## 📊 Примеры использования

### Пример 1: Простая функция

```python
from global_optimizer import GlobalOptimizer
from visualizer import Visualizer

# Создаем оптимизатор
optimizer = GlobalOptimizer(
    function_str="x**2 - 4*x + 4",  # f(x) = (x-2)²
    a=0.0,
    b=5.0,
    eps=0.01
)

# Запускаем оптимизацию
result = optimizer.optimize()

print(f"Минимум в точке x = {result['x_min']:.4f}")
print(f"Значение f(x) = {result['f_min']:.4f}")
print(f"Итераций: {result['iterations']}")

# Визуализация
vis = Visualizer(optimizer)
vis.plot_results(save_path='my_result.png')
```

### Пример 2: Функция Растригина

```python
# Функция с множеством локальных минимумов
optimizer = GlobalOptimizer(
    function_str="10 + x**2 - 10*np.cos(2*np.pi*x)",
    a=-5.0,
    b=5.0,
    eps=0.01
)

result = optimizer.optimize()
# Глобальный минимум: x* = 0, f(x*) = 0
```

### Пример 3: Функция Экли

```python
optimizer = GlobalOptimizer(
    function_str="-20*np.exp(-0.2*np.abs(x)) - np.exp(np.cos(2*np.pi*x)) + 20 + np.e",
    a=-5.0,
    b=5.0,
    eps=0.01
)

result = optimizer.optimize()
# Глобальный минимум: x* = 0, f(x*) = 0
```

## 📁 Структура проекта

```
lab2opt/
│
├── global_optimizer.py    # Основной алгоритм оптимизации
├── visualizer.py          # Модуль визуализации результатов
├── test_functions.py      # Тесты с Растригином, Экли и др.
├── main.py               # Интерактивный режим
├── requirements.txt      # Зависимости проекта
├── README.md            # Документация (этот файл)
│
└── results/             # Директория с результатами
    ├── rastrigin_result.png
    ├── rastrigin_convergence.png
    ├── ackley_result.png
    ├── ackley_convergence.png
    └── ...
```

## 🔧 Описание модулей

### `global_optimizer.py`

**Класс `GlobalOptimizer`** - основной класс для оптимизации.

**Основные методы:**
- `__init__(function_str, a, b, eps)` - инициализация с параметрами
- `optimize(max_iterations)` - запуск оптимизации
- `_estimate_lipschitz_constant()` - оценка константы Липшица
- `_compute_auxiliary_minimum()` - вычисление минимума вспомогательной функции

**Возвращаемый результат:**
```python
{
    'x_min': float,      # Найденный аргумент минимума
    'f_min': float,      # Найденное значение минимума
    'iterations': int,   # Число итераций (вызовов функции)
    'time_spent': float, # Время в секундах
    'success': bool      # True если достигнута точность
}
```

### `visualizer.py`

**Класс `Visualizer`** - визуализация результатов.

**Методы:**
- `plot_results()` - основной график с функцией, точками, ломаной и вспомогательными функциями
- `plot_convergence()` - график сходимости (как улучшается минимум)
- `create_animation_frames()` - создание кадров для анимации процесса

### `test_functions.py`

Содержит готовые тесты для демонстрации работы:
- `run_rastrigin_test()` - тест на функции Растригина
- `run_ackley_test()` - тест на функции Экли
- `run_custom_test()` - тест на пользовательской функции
- `compare_all_tests()` - запуск всех тестов и сравнение

## 📈 Результаты

### Функция Растригина

**Функция:** `f(x) = 10 + x² - 10·cos(2πx)`

**Характеристики:**
- Множество локальных минимумов (периодические "ямы")
- Глобальный минимум: `x* = 0`, `f(x*) = 0`
- Сложная для локальных методов оптимизации

**Результат:**
- Найденный минимум: `x* ≈ 0.000xxx`, `f(x*) ≈ 0.00xxx`
- Точность: высокая (ошибка < 0.01)

![Растригин](results/rastrigin_result.png)

### Функция Экли

**Функция:** `f(x) = -20·exp(-0.2·|x|) - exp(cos(2πx)) + 20 + e`

**Характеристики:**
- "Волнистая" структура с экспоненциальным спадом
- Глобальный минимум: `x* = 0`, `f(x*) = 0`
- Много локальных минимумов

**Результат:**
- Найденный минимум: `x* ≈ 0.000xxx`, `f(x*) ≈ 0.00xxx`
- Точность: высокая

![Экли](results/ackley_result.png)

## ❓ Вопросы и ответы для защиты

### 1. Что такое липшицевость и почему она важна?

**Ответ:** Липшицевость означает, что функция не может изменяться быстрее чем линейно с некоторым коэффициентом L. Это важно, потому что:
- Позволяет строить гарантированные нижние оценки функции
- Обеспечивает сходимость метода к глобальному минимуму
- Исключает бесконечно резкие "скачки" функции

### 2. Как оценивается константа Липшица в программе?

**Ответ:** См. метод `_estimate_lipschitz_constant()` в `global_optimizer.py:67-102`. Оценка производится как:
```
L = r · max{|f(xᵢ) - f(xⱼ)| / |xᵢ - xⱼ|}
```
по всем парам точек. Коэффициент `r > 1` добавляет "запас прочности".

### 3. Что такое вспомогательные функции?

**Ответ:** Это функции `R(x)`, которые аппроксимируют исходную функцию снизу. Строятся как максимум двух "конусов":
```
R(x) = max{f(xᵢ) - L·(x - xᵢ), f(xᵢ₊₁) - L·(xᵢ₊₁ - x)}
```
См. метод `_compute_auxiliary_minimum()` в `global_optimizer.py:104-138`.

### 4. Как выбирается следующая точка для вычисления?

**Ответ:** Выбирается точка с минимальным значением вспомогательной функции среди всех отрезков. Это точка пересечения двух конусов:
```
x* = (xᵢ + xᵢ₊₁)/2 - (f(xᵢ₊₁) - f(xᵢ))/(2L)
```
См. строки 169-186 в `global_optimizer.py`.

### 5. Какой критерий останова используется?

**Ответ:** Алгоритм останавливается, когда разность между минимумом функции и минимумом вспомогательной функции меньше `ε`:
```
min{f(xᵢ)} - min{R(x)} < ε
```
См. строки 198-203 в `global_optimizer.py`.

### 6. Почему метод гарантирует нахождение глобального минимума?

**Ответ:**
- Вспомогательные функции всегда ниже истинной функции (из-за липшицевости)
- Минимум R(x) даёт нижнюю оценку глобального минимума
- По мере добавления точек "зазор" между f(x) и R(x) уменьшается
- Когда зазор < ε, мы гарантированно близки к глобальному минимуму

### 7. Чем этот метод отличается от градиентных методов?

**Ответ:**
- **Глобальный поиск** vs локальный (градиентные методы застревают в локальных минимумах)
- **Не требует производных** - только значения функции
- **Гарантия сходимости** к глобальному минимуму
- **Недостаток**: больше вычислений функции

### 8. Что означает параметр r в оценке L?

**Ответ:** Коэффициент надежности (обычно 1.5-3.0). Увеличивает оценку константы Липшица для "подстраховки". Если r слишком мал, можем пропустить минимум. Если слишком велик - медленная сходимость.

## 📝 Создание PDF отчета

Для создания PDF с результатами можно использовать:

### Вариант 1: Печать в PDF из браузера
```bash
# Установите pandoc (если нужен markdown -> PDF)
# Или просто откройте README.md + графики в браузере и "Печать → PDF"
```

### Вариант 2: LaTeX
```bash
# Создайте LaTeX документ с включением графиков из results/
pdflatex report.tex
```

### Вариант 3: Python скрипт
```python
# Используйте matplotlib для создания многостраничного PDF
from matplotlib.backends.backend_pdf import PdfPages

with PdfPages('report.pdf') as pdf:
    # Добавьте все графики
    # ...
```

## 🎯 Для сдачи преподавателю

**Что нужно предоставить:**

1. ✅ **Код программы** - весь репозиторий или ZIP архив
2. ✅ **Демонстрация работы** - результаты в `results/`
3. ✅ **Визуализация** - графики функций и сходимости
4. ✅ **Документация** - этот README.md с теорией
5. ✅ **PDF отчет** - создать из README.md + графиков

**Демонстрируемые функции:**
- ✅ Функция Растригина (>2 локальных минимума на отрезке)
- ✅ Функция Экли (>2 локальных минимума)
- ✅ Пользовательская функция

**Выводимая информация:**
- ✅ Приближенное значение `x*`
- ✅ Приближенное значение `f(x*)`
- ✅ Число итераций
- ✅ Затраченное время
- ✅ Визуализация функции
- ✅ Визуализация ломаной линии
- ✅ Визуализация вспомогательных функций
- ✅ Указание найденного минимума на графике

## 📧 Контакты и помощь

При возникновении вопросов изучите:
1. Комментарии в коде (очень подробные!)
2. Этот README.md
3. Раздел "Вопросы и ответы для защиты"

## 📜 Лицензия

Учебный проект. Свободное использование для образовательных целей.

---

**Удачи на защите лабораторной работы! 🎓**
